# --- Secrets & Environment ---
.env
*.pem
*.key
credentials/

# --- Local Container Data ---
jenkins_home/
.docker/
docker-compose.override.yml

# --- Terraform (state, provider plugins, plans) ---
*.tfstate
*.tfstate.*
.terraform/
tfplan

# --- OS ---
.DS_Store

# Terraform variable files may hold secrets (admin password, CIDRs)
*.tfvars
*.tfvars.json
